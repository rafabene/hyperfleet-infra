{{- if .Values.deployment.podDisruptionBudget.enabled }}
{{- if and .Values.deployment.podDisruptionBudget.minAvailable .Values.deployment.podDisruptionBudget.maxUnavailable }}
{{- fail "deployment.podDisruptionBudget: cannot set both minAvailable and maxUnavailable" }}
{{- end }}
{{- if not (or .Values.deployment.podDisruptionBudget.minAvailable .Values.deployment.podDisruptionBudget.maxUnavailable) }}
{{- fail "deployment.podDisruptionBudget.enabled is true but neither minAvailable nor maxUnavailable is set" }}
{{- end }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "hyperfleet-gateway.fullname" . }}
  labels:
    {{- include "hyperfleet-gateway.labels" . | nindent 4 }}
spec:
  {{- if and .Values.deployment.podDisruptionBudget.minAvailable (not .Values.deployment.podDisruptionBudget.maxUnavailable) }}
  minAvailable: {{ .Values.deployment.podDisruptionBudget.minAvailable }}
  {{- end }}
  {{- if and .Values.deployment.podDisruptionBudget.maxUnavailable (not .Values.deployment.podDisruptionBudget.minAvailable) }}
  maxUnavailable: {{ .Values.deployment.podDisruptionBudget.maxUnavailable }}
  {{- end }}
  {{- if .Values.deployment.podDisruptionBudget.unhealthyPodEvictionPolicy }}
  unhealthyPodEvictionPolicy: {{ .Values.deployment.podDisruptionBudget.unhealthyPodEvictionPolicy }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "hyperfleet-gateway.selectorLabels" . | nindent 6 }}
{{- end }}
